s=SELECT
f=FROM
w=WHERE
o=ORDER BY
dl=DELETE
sf=SELECT * FROM
df=DELETE FROM
sc=SELECT COUNT(*) FROM
g=GROUP BY
h=HAVING
tt=TRUNCATE TABLE
dr=DROP TABLE
ct=CREATE TABLE
sd=SELECT DISTINCT
ei=EXECUTE IMMEDIATE

ll=SELECT S.USERNAME,DECODE(L.TYPE, 'TM', 'TABLE LOCK', 'TX', 'ROW LOCK', NULL) LOCK_LEVEL,O.OWNER, O.OBJECT_NAME,O.OBJECT_TYPE,S.SID, S.SERIAL#, S.TERMINAL, S.MACHINE, S.PROGRAM,S.OSUSER  FROM V$SESSION S, V$LOCK L, DBA_OBJECTS O WHERE L.SID = S.SID   AND L.ID1 = O.OBJECT_ID(+) AND S.USERNAME IS NOT NULL

kp=SELECT A.SPID,B.SID,B.SERIAL#,B.USERNAME FROM V$PROCESS A,V$SESSION B WHERE A.ADDR = B.PADDR AND B.STATUS = 'KILLED'--查看处于Killed状态的进程

ki=ALTER SYSTEM KILL SESSION '143,125';

re=SELECT 'ALTER database TEMPFILE  ''' || FILE_NAME || ''' resize 100M ;' FROM DBA_TEMP_FILES;

vp=select a.SID,a.SCHEMANAME, a.OSUSER,a.USERNAME,a.STATUS,a.EVENT, a.MACHINE,a.ACTION,b.SPID from v$session a,v$process b where a.PADDR=b.ADDR and A.SID=

sp=select TABLESPACE_NAME,round(USED_SPACE * 8 / 1024 / 1024,2) USED_SPACE,round(TABLESPACE_SIZE * 8 / 1024 / 1024,2) TABLESPACE_SIZE,round(USED_PERCENT,2) USED_PERCENT from dba_tablespace_usage_metrics order by used_percent;